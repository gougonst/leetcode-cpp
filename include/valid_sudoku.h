#include <vector>
using namespace std;

class Solution {
public:
    static const int N_ELE = 9;
    bool isValidSudoku1(vector<vector<char>>& board);
    bool isValidSudoku2(vector<vector<char>>& board);
    bool isValidSudoku3(vector<vector<char>>& board);
};
